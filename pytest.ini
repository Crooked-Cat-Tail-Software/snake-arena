[pytest]
markers =
    integration: requires Docker Desktop / a Docker Engine with the Compose v2 plugin (see tests/integration/conftest.py). Not run by the default `pytest tests -v` command -- run explicitly with `pytest tests/integration -v`.
